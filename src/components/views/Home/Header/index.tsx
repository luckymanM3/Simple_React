import styled from "styled-components";

const HeaderContent = styled.div`
  height: 100px;
  background: #5183bf;
`;

export const Header: React.FC = () => {
  return (
    <HeaderContent >
      
    </HeaderContent>
  )
}